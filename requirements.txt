# Lightweight trading stack — only packages imported by the app.
# Transitive deps (urllib3, certifi, numpy, etc.) install automatically.

# Web / orchestration (main.py, keep_alive.py, tracker_agent.py)
flask==3.1.3
requests==2.32.3

# Market data (master_bot.py, data_engineer.py, tracker_agent.py, …)
yfinance==0.2.54
pandas==2.2.3
pytz==2025.2

# LLM clients (master_bot / managers / ticker_desk)
google-genai==1.75.0
groq==0.28.0
