# Copy to .env and fill in real values. Never commit .env.

GEMINI_API_KEY=
DEEPSEEK_API_KEY=
DISCORD_WEBHOOK=

# Optional model overrides (defaults are free-tier safe)
LLM_GEMINI_MODEL=gemini-flash-latest
TRACKER_GEMINI_MODEL=gemini-flash-latest
